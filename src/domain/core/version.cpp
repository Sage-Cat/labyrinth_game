
#include "domain/core/version.hpp"
// Intentionally empty: inline constexpr in header provides the API.
// Translation unit ensures library target is not empty and links consistently across toolchains.
